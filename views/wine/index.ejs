<h1>Wines</h1>

<table>
    <tr>
        <th>Name</th>
        <th>Grape</th>
        <th>Region</th>
        <th>Country</th>
        <th> Created At </th>
        <th> Updated At </th>
        <th>Edit</th>
    </tr>

    <% wines.forEach(function(wine) { %>
    
        <tr>
            <td><a href="/wine/detail?id=<%= wine._id %>"><%= wine.name %></a></td>
            <td><%= wine.grape %></td>
            <td><%= wine.region %></td>
            <td><%= wine.country %></td>
            <td><%= moment(wine.createdAt).fromNow() %></td>
            <td><%= moment(wine.updatedAt).fromNow() %></td>
            <td><a href="/wine/edit?id=<%= wine._id %>">Edit</a></td>
        </tr>

    <% }) %>

</table>